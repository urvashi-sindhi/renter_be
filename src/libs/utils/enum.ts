export enum SwaggerConfig {
  TITLE = 'Renter Application',
}

export enum ResponseStatus {
  SUCCESS = 'Success',
  ERROR = 'Error',
}

export enum ApiTag {
  USER = 'User',
}

export enum Role {
  RENTER = 'Renter',
  LANDLORD = 'Landlord',
}

export enum Sharing {
  SINGLE = 'Single',
  DOUBLE = 'Double',
  TRIPLE = 'Triple',
}
